/**
 * TypeScript Types for MemoryMosaic Premium
 *
 * Auto-generated by Claudery Blueprint Generator
 */

// ============================================
// USER & AUTH
// ============================================

export interface User {
  id: string;
  email: string;
  username?: string;
  displayName?: string;
  avatarUrl?: string;
  role: "user" | "admin";
  createdAt: Date;
  updatedAt: Date;
}

export interface Profile {
  id: string;
  username?: string;
  displayName?: string;
  avatarUrl?: string;
  bio?: string;
  createdAt: Date;
  updatedAt: Date;
}

export interface Session {
  user: User;
  accessToken: string;
  expiresAt: Date;
}

// ============================================
// ENTITIES
// ============================================

export interface Idea {
  id: string;
  userId: string;
  id: string;
  created_at: Date;
  updated_at: Date;
  user_id: string;
  title: string;
  description?: string;
  createdAt: Date;
  updatedAt: Date;
}

export type IdeaCreateInput = Omit<Idea, "id" | "userId" | "createdAt" | "updatedAt">;
export type IdeaUpdateInput = Partial<IdeaCreateInput>;

export interface Story {
  id: string;
  userId: string;
  id: string;
  created_at: Date;
  updated_at: Date;
  user_id: string;
  title: string;
  content: string;
  createdAt: Date;
  updatedAt: Date;
}

export type StoryCreateInput = Omit<Story, "id" | "userId" | "createdAt" | "updatedAt">;
export type StoryUpdateInput = Partial<StoryCreateInput>;

export interface User {
  id: string;
  userId: string;
  id: string;
  created_at: Date;
  updated_at: Date;
  user_id: string;
  name: string;
  email: string;
  password: string;
  createdAt: Date;
  updatedAt: Date;
}

export type UserCreateInput = Omit<User, "id" | "userId" | "createdAt" | "updatedAt">;
export type UserUpdateInput = Partial<UserCreateInput>;

// ============================================
// API RESPONSES
// ============================================

export interface ApiResponse<T> {
  data?: T;
  error?: {
    code: string;
    message: string;
    details?: unknown;
  };
}

export interface PaginatedResponse<T> {
  data: T[];
  pagination: {
    total: number;
    limit: number;
    offset: number;
    hasMore: boolean;
  };
}

// ============================================
// COMMON TYPES
// ============================================

export type SortOrder = "asc" | "desc";

export interface SortOptions {
  field: string;
  order: SortOrder;
}

export interface FilterOptions {
  search?: string;
  status?: string;
  dateFrom?: Date;
  dateTo?: Date;
}

export interface QueryOptions {
  limit?: number;
  offset?: number;
  sort?: SortOptions;
  filters?: FilterOptions;
}

// ============================================
// FORM TYPES
// ============================================

export interface FormState<T> {
  values: T;
  errors: Partial<Record<keyof T, string>>;
  touched: Partial<Record<keyof T, boolean>>;
  isSubmitting: boolean;
  isValid: boolean;
}

// ============================================
// UTILITY TYPES
// ============================================

export type WithId<T> = T & { id: string };
export type WithTimestamps<T> = T & { createdAt: Date; updatedAt: Date };
export type Nullable<T> = T | null;
export type Optional<T, K extends keyof T> = Omit<T, K> & Partial<Pick<T, K>>;
